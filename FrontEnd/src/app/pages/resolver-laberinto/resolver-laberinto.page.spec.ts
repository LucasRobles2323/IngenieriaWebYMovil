import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ResolverLaberintoPage } from './resolver-laberinto.page';

describe('ResolverLaberintoPage', () => {
  let component: ResolverLaberintoPage;
  let fixture: ComponentFixture<ResolverLaberintoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ResolverLaberintoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
